require "spec_helper"

describe ZhimaAuth do
  it "has a version number" do
    expect(ZhimaAuth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
